# 一番初めのpaneを選択
select-pane -t 0
# 左右にウィンドウを分割
split-window -h
# 0番目(左側)のウィンドウを選択
select-pane -t 0
# 上下にウィンドウを分割する
split-window -v

# メインとなるウィンドウの高さを25行に設定
#setw main-pane-height 25
# 左右分割レイアウトを反映
#select-layout main-virtical
# 上下分割レイアウトを反映
#select-layout main-horizontal
display-panes

# 2番目(右)のpaneの横幅（境界)を右に22文字幅移動する
resize-pane -L -t 2 22
